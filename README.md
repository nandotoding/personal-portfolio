## Personal Portfolio Website

This website uses a Public API that requires a token as its API key.\
Get your API Token on stockdata.org.\
Create a .env file in the root of project and store your\
API token in it, you should name its variable `REACT_APP_API_TOKEN`.

The app will not be run properly if you do not include an API token in it.